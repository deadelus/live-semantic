# TODO — LiveSemantic

Backlog dérivé des décisions d'architecture validées (voir contexte de mission). Chaque tâche référence sa ligne dans `AUDIT.md` § Étape 2bis. Les tâches marquées **[DÉJÀ FAIT → À INTÉGRER]** ne sont pas à recoder : le travail existe mais est non commité (stash) ou non mergé (branche) — le premier geste est de le récupérer, pas de le réécrire.

Ordre de dépendance global : **G (hygiène) → E (ports) → B (IoU/tracker) → D (Track) → C (async) → A (cascade) → F (latences, en continu) → H (GUI web + desktop)**. H a désormais sa propre section ci-dessous (planifiée le 2026-08-10, pas encore commencée) — **backend d'abord** (H1), rien côté UI (H2/H3) tant que H1 n'avance pas.

---

## ✅ Bug critique SIGABRT — résolu le 2026-08-10 (root-cause confirmée + fix)

Découvert le 2026-08-09 en construisant l'outil de test de dérive (`cmd/tracking-drift-bench`), hors de tout scope prévu. Le repro minimal initial pointait vaguement vers "un conflit entre destructeurs statiques C++ ORT/gocv" — **c'était trop large, l'un des deux n'y est pour rien.**

**Root-cause isolée avec `lldb`** (`bt all` sur le process qui crashe, reproductible 100% du temps avec juste `yolo11s.New()` + `Cleanup()`, gocv pas nécessaire pour reproduire — juste présent dans le repro d'origine par coïncidence de code) :
```
frame #8: onnxruntime_amd64.dylib`__clang_call_terminate
frame #9: onnxruntime_amd64.dylib`std::__1::unique_ptr<OrtEnv,...>::~unique_ptr()
```
Le singleton global `OrtEnv` d'ONNX Runtime est détruit via un **destructeur statique C++** à la sortie du process (jamais explicitement relâché par notre code — `runtime.InitEnvironment` appelle `InitializeEnvironment()` mais rien n'appelait `DestroyEnvironment()`). Ce destructeur lève une exception non interceptée (`mutex lock failed: Invalid argument`) → `std::terminate()` → `SIGABRT`.

**Bug connu et documenté côté ONNX Runtime lui-même**, pas notre code : [microsoft/onnxruntime#24579](https://github.com/microsoft/onnxruntime/issues/24579), touche 1.21.x-1.22.x sur macOS (la lib bundlée ici est en 1.22.0, confirmé via `otool -L`). Corrigé upstream par [PR #26445](https://github.com/microsoft/onnxruntime/pull/26445) — mais uniquement côté binding **Node.js** (`js/node/src/ort_singleton_data.cc`), pas le cœur de l'API C que notre binding Go (cgo direct) appelle. Le fix upstream ne nous couvre donc pas automatiquement.

**Fix appliqué** : `runtime.DestroyEnvironment()` (nouveau, `internal/implementation/inference/onnx/runtime/session.go`) appelle explicitement `ort.DestroyEnvironment()` — appelé depuis `Detector.Cleanup()`. Confirmé 100% reproductible dans les deux sens en isolant avec `lldb`/binaire de test dédié : 3/3 crashs sans l'appel explicite, 5/5 sorties propres avec, sur le vrai `Detector.Cleanup()` du projet (pas juste le repro isolé).

- [x] Confirmer/infirmer le crash — confirmé indirectement : le crash n'a jamais été observé sur les runs webcam réels de l'utilisateur (`echo $?` = 0 à 3 reprises), mais le repro isolé plantait à 100%. Explication la plus probable après coup : les runs réels n'ont peut-être jamais atteint un `Cleanup()` complet avant que le shell ne récupère le code de sortie (terminaison par signal externe au process Go plutôt que retour propre de `main`), ou une différence d'environnement non identifiée — non élucidé, mais désormais sans conséquence puisque le vrai bug est corrigé indépendamment de ce point.
- [x] Root-cause isolée précisément via `lldb bt all` sur un repro minimal — pas de bisection nécessaire, la pile d'appel a directement pointé vers `OrtEnv`'s destructeur statique dans `onnxruntime_amd64.dylib`.
- [x] Corrigé : `runtime.DestroyEnvironment()` + appel dans `Detector.Cleanup()`. Testé 3x sans fix (crash systématique) vs 5x avec fix (propre systématique), même binaire.

## G — Restructuration & hygiène (bloquant, faible risque, à faire en premier)

Référence matrice : G — restructuration absente partout, problèmes d'hygiène confirmés en étape 2. **Phase terminée le 2026-08-05** (voir `MIGRATION.md` § Phase 1 pour le détail de ce qui a réellement été fait, la structure finale diffère légèrement de la proposition initiale — affinée avec l'utilisateur avant exécution).

- [x] `git rm --cached .env`, créer `.env.example` avec les 4 clés sans valeurs sensibles. `.env` et `.DS_Store` ajoutés à `.gitignore`.
- [x] Ajouter `LICENSE` (MIT).
- [x] Remplacer les placeholders `github.com/your-org/livesemantic` par `github.com/deadelus/live-semantic` dans `readme.md`.
- [x] Corriger les promesses de latence non étayées ("sub-50ms") dans `readme.md`.
- [ ] Purger les `*.onnx` de l'**historique** Git (réécriture via `git filter-repo`/BFG), sans passer par Git LFS pour l'instant — décision : on ignore la piste LFS/téléchargement-au-build pour le moment, on flush directement. **Pas fait** — ils sont déjà regroupés proprement dans `assets/` (fait) pour l'état courant, mais l'historique Git existant n'a pas été réécrit. **Attention avant exécution** : un `filter-repo --invert-paths` naïf supprimerait aussi les `.onnx` de l'état courant (aucun mécanisme de téléchargement au build n'existe encore) — il faudra une réécriture qui préserve le blob dans le commit le plus récent, ou un remplacement par un stub + téléchargement, sinon le build casse. Le poids du `.git` (136 Mo, `MIGRATION.md` § Risques) s'est encore alourdi : le stash `yoloe11s-seg.onnx` (41 Mo) récupéré sur `recover/yoloe11s-seg-stash` doit être inclus dans le périmètre de la purge.
- [x] Migration `src/` → `cmd/livesemantic/` + `internal/`. Structure finale (affinée avec l'utilisateur, différente de la proposition initiale `ports/`+`infrastructure/`) :
  `internal/domain/entities` (pur, ex-`model/`, renommé) · `internal/application/{uc,dto}` (orchestre domaine + ports) · `internal/infrastructure/*` (interfaces/ports, nom conservé du code existant) · `internal/implementation/*` (adapters concrets, nom conservé, `drawer/` y a rejoint) · `internal/transport/{handlers,envelopes,adapters/{api,cli,cmd,websocket}}` (raffiné a posteriori, voir `MIGRATION.md`) · `cmd/livesemantic/main.go`. Assets binaires (fonts, modèles ONNX, libs natives) sortis du code source vers `assets/` à la racine, chemins hardcodés mis à jour en conséquence. Build/vet/test/smoke-test tous verts après coup.
- [x] Déplacer les DTO vers la couche applicative — fait (`internal/application/dto`), en même temps que la migration ci-dessus plutôt qu'en deux passes.

## E — Ports (inversion de dépendance complète)

Référence matrice : E — **Phase terminée le 2026-08-06.** Les 5 ports cibles existent (`ObjectDetector`, `ObjectTracker`, `SemanticEncoder`, `AlertSender`, `MetricsCollector`) avec la forme voulue (pas de fuite vendor). `ObjectTracker`/`SemanticEncoder`/`MetricsCollector` sont des interfaces sans implémentation câblée — normal, ils dépendent respectivement de B, A, et de points d'instrumentation qui n'existent pas encore.

- [x] Renommer `infrastructure/ai.AI` en `infrastructure/inference.ObjectDetector` (fait le 2026-08-05, avant même le reste de la phase E — l'occasion s'est présentée en réorganisant `implementation/ai` → `implementation/inference/onnx/{runtime,yolo11s}`).
- [x] Corriger la fuite d'abstraction — **fait de facto le 2026-08-05, effet de bord du commit `4cad31d`** (remplacement de `go-clean-onnxruntime` par `onnxruntime_go`) : `decodeDetections` (`yolo11s.go`) construit directement `[]entities.BoundingBox` (`Label`, `Confidence`, `X1/Y1/X2/Y2`), plus aucune trace de `onnx.BoundingBox` dans le code (vérifié par grep le 2026-08-06). `DetectionResult.BoundingBoxes []entities.BoundingBox` est déjà un type domaine propre. Item non coché jusqu'ici par oubli de mise à jour de cette TODO après le refacto — pas un travail restant.
- [x] Créer `ObjectTracker` (`internal/infrastructure/tracking/tracker.go`) : `Init(frame, box) error` / `Update(frame) (entities.BoundingBox, bool)`. Interface seule, non implémentée (**dépend du choix du tracker gocv, voir B**), non câblée dans `main.go`/`UseCase` — rien à instancier tant qu'aucun adapter n'existe.
- [x] Créer `SemanticEncoder` (`internal/infrastructure/inference/semantic_encoder.go`) : `EncodeImage(frame)`/`EncodeText(text)` → `entities.Embedding` (nouveau type, `internal/domain/entities/embedding.go`, `[]float32`). Interface seule, non implémentée (**dépend du backend CLIP, voir A**), non câblée.
- [x] `AlertSender` : `notifier.Notifier` renommé `notifier.AlertSender` (`internal/infrastructure/notifier/notification.go`). Contrat inchangé (`Notify(msg entities.Message) error` + `Cleanup()`) — il correspondait déjà à la cible, pas d'adaptation nécessaire. `LogNotifier` et tous les appelants (`uc.UseCase`, `main.go`) mis à jour.
- [x] Créer `MetricsCollector` (`internal/infrastructure/metrics/metrics.go`) : `RecordLatency(stage, duration)` / `IncrementCounter(name)`. Implémentation console fournie (`internal/implementation/metrics/console-metrics`, `ConsoleMetrics`, logge via `logger.Logger` injecté). **Non câblée dans `UseCase`/`main.go`** : aucun point d'instrumentation n'existe encore dans le pipeline (rien à mesurer avant B/C), le câblage est un suivi à faire quand il y aura une latence réelle à observer — pas un travail perdu, juste prématuré de forcer un appel sans rien à instrumenter.

## B — Tracking-by-detection

Référence matrice : B — **boucle de ré-ancrage vivante depuis le 2026-08-09**, branchée dans `main.go`/`UseCase`. Seul le test de dérive KCF vs CSRT reste ouvert (nécessite du matériel vidéo réel, pas juste du code).

- [x] Réutiliser l'IoU existant plutôt que le réécrire : fait le 2026-08-05, en même temps que la sortie de `go-clean-onnxruntime` (voir `github.com/deadelus/go-clean-onnxruntime` vs `onnxruntime_go`, décision prise en aparté) — `entities.BoundingBox.IoU/Intersection/Union` dans `internal/domain/entities/boundingbox.go`, utilisé pour le NMS de `yolo11s.go`. Publique, testable, plus de dépendance au vendor pour ça.
- [x] Adapter KCF/CSRT derrière `infrastructure/tracking.ObjectTracker` — `internal/implementation/tracking/gocv-tracker` (`Tracker`, algo sélectionnable via `New(KCF|CSRT)`, un seul adapter pour les deux : le choix reste ouvert tant que le test de dérive n'est pas fait). Vendoring `gocv.io/x/gocv/contrib` ajouté (`go mod vendor`). Tests unitaires (`tracker_test.go`, table-driven) : construction, erreurs, `Cleanup()` idempotent avant `Init`. **MIL non adapté** (réserve si KCF/CSRT déçoivent, cf. `docs/adr/object-tracking.md` § 5) — pas branché dans `main.go`/`UseCase` : rien ne consomme encore `ObjectTracker` dans le pipeline (voir item suivant).
- [x] Boucle de ré-ancrage périodique + association IoU contre les tracks existantes — `internal/application/uc/tracking.go` (`trackManager`). Re-détection YOLO toutes les `reanchorInterval=45` frames (~1.5s à 30fps, **pas mesuré**, cf. F) plutôt que "sur chute de confiance" (aurait demandé d'exposer la confiance du tracker lui-même, que gocv ne fournit pas — KCF/CSRT ne renvoient qu'un booléen succès/échec, pas de score). Association gloutonne par IoU (seuil `iouAssociationThreshold=0.4`, milieu de la fourchette 0.3-0.5), restreinte à la même classe, pas d'algorithme hongrois (suffisant pour une v1). `RecognitionUseCase` réécrit pour consommer `trackManager` au lieu de redétecter à chaque frame — corrige au passage un bug latent (les events clavier n'étaient jamais lus si `BoundingBoxes` était `nil`, `Escape` pouvait rester sans effet) et réactive le matching de filtre + `notifier.Notify`, qui était mort en commentaire depuis le début.
- [x] Test de dérive KCF vs CSRT sur vidéo réelle — `cmd/tracking-drift-bench` (outil headless jetable), résultats et décision dans `docs/adr/object-tracking.md` § 7-8. **KCF confirmé comme défaut** (avg IoU supérieur à CSRT sur les deux vidéos testées, `person.mp4` + `car.mp4`), déjà le choix câblé dans `main.go`. Échantillon petit (2 vidéos) — pas définitif, CSRT reste à un changement d'une ligne si besoin.

## D — Agrégat `Track`

Référence matrice : D — **type domaine + événements faits le 2026-08-09.** Le 3e item (scission `SemanticFilter`) reste hors scope tant que A n'a pas de CLIP qui tourne, volontairement non commencé.

- [x] Créer le type domaine `Track` (`internal/domain/entities/track.go`) : `ID`, `Class`, `Trajectory []BoundingBox`, `Embeddings []Embedding` (vide tant que A n'existe pas), `FirstSeen`/`LastSeen`, machine à états `StateTentative → StateConfirmed → StateCoasting → StateLost` via `MatchDetection`/`Coast`/`Miss`. Seuils `minHitsToConfirm=3`/`maxMissesBeforeLost=5` posés en dur (valeurs SORT-like par défaut, **non mesurées** — à revoir une fois la boucle de ré-ancrage tournant sur vidéo réelle, cf. F). 11 tests table-driven (`track_test.go`), 100% des transitions couvertes.
- [x] Émettre les événements `TrackEntered`/`TrackMatched`/`TrackLost` — mécanisme créé (`MatchDetection`/`Miss` retournent un `*TrackEvent`, pur/sans I/O côté domaine) **et câblé** : `trackManager.emit` (`internal/application/uc/tracking.go`) route vers `AlertSender.Notify` quand la classe du track correspond au filtre demandé (`req.Filter`) au-dessus du seuil (`req.SimilarityThreshold`). `EventTrackLost` est loggé mais n'alerte jamais (rien à notifier sur une disparition).
- [ ] Scinder `SemanticFilter` en filtres **instantanés** (crop) et filtres **temporels** (règles sur trajectoire). **Dépendance : Track + décision A (CLIP) pour les filtres instantanés. Effort : M, et dépend fortement de la forme finale de A — ne pas figer le design avant d'avoir un CLIP qui tourne.**
- [ ] **Décidé le 2026-08-10, pas commencé** : troisième type de filtre instantané — reconnaissance **par référence image** plutôt que par texte. Sélectionner une box (détectée ou dessinée) dans le flux vidéo, lui donner un label, `EncodeImage` sur ce crop → embedding stocké dans une petite galerie locale (`{label, embedding}`, en mémoire, pas de base vectorielle à cette échelle). Chaque détection suivante compare son crop (similarité cosinus) contre la galerie plutôt que contre un filtre texte. **N'a besoin que d'`EncodeImage`, pas d'`EncodeText`/tokenizer** — plus simple que le filtre texte, mais nécessite une interaction UI pendant que le flux tourne (rien de prévu dans le CLI actuel, question/réponse avant lancement uniquement). **Dépendance : CLIP intégré (texte d'abord, décision utilisateur) + UI d'interaction en direct à concevoir. Effort : S une fois CLIP prêt côté image, le vrai effort est l'UI.** **Recoupe § H1 "Galerie de références + sélection runtime"** — la "UI d'interaction en direct" dont ce point dépend, c'est exactement ce que H2 fournit une fois H1 câblé. Un seul chantier : le *quoi/pourquoi* reste documenté ici, le suivi d'exécution passe par § H1.

## C — Découplage async 3 boucles

Référence matrice : C — **vidéo + détection découplées le 2026-08-10.** `feat/ochestrator` n'existe plus (branche supprimée, décision actée plus haut dans ce fichier) — réécrit à neuf, pas récupéré de ce prototype.

- [x] Réécrire la boucle vidéo pour qu'elle ne bloque jamais (lecture des résultats de détection en `select`/`default`) — `uc_recognition.go`. La boucle vidéo n'appelle plus jamais l'inférence YOLO ; elle fait juste `tracks.advance()` (rapide, ~10-20ms) + rendu, et pousse la frame courante vers la boucle de détection sans jamais attendre.
- [x] Channel de frames à détecter en buffer 1 avec écrasement — implémenté directement dans `RecognitionUseCase` (`frameChan := make(chan *entities.Frame, 1)` + `select`/`default` à l'envoi, vide-et-remplace si plein). Pas de code récupéré d'un prototype (la branche n'existe plus), réécrit proprement.
- [x] Boucle de détection séparée sur sa propre goroutine — `tracks.reanchor()` tourne en continu sur un goroutine dédié, consomme `frameChan`. **Pas de cadence 2-5 FPS forcée explicitement** : le coût de `reanchor` lui-même (~150-270ms mesuré) autorégule naturellement la cadence dans cette fourchette, pas de ticker ajouté — à revoir si le matériel devient assez rapide pour que ça ne suffise plus.
- [x] **Supersédé le 2026-08-10 par la décision A (option a).** L'idée d'origine (CLIP en 3e boucle async, déclenché 1× à la confirmation du track) supposait que CLIP serve de *tag sémantique* sur un track déjà confirmé par YOLO+tracker seul. Ce n'est plus le design retenu : CLIP est désormais le **gate d'admission** lui-même (`trackManager.reanchor`, `internal/application/uc/tracking.go`) — un candidat n'est même associé à un track (`bestMatch`/`MatchDetection`) que s'il a déjà passé le filtre sémantique. Or `minHitsToConfirm=3` (`track.go`) exige 3 passes de `MatchDetection` consécutives pour confirmer un track — donc CLIP tourne nécessairement 3× (une fois par candidat par passe de `reanchor`) *avant* même qu'un track existe à l'état confirmé, pas 1× après — incompatible avec "1× par track confirmée, pas par frame" tel qu'écrit ici à l'origine. Pas un oubli — une conséquence directe et assumée du choix (a) plutôt que (b) (fallback). Coût réel de ce choix : voir benchmark ci-dessous (§ F).
- [x] V1 recalage naïf (le tracker rattrape le décalage) — implicite dans le design ci-dessus : `reanchor` peut traiter une frame vieille de quelques dizaines de ms, `advance` (boucle vidéo, continue) rattrape le décalage entre-temps. Pas mesuré précisément en usage réel (pas de webcam ici) — comportement correct par construction, à confirmer.
- [ ] V2 ring buffer des ~15 dernières frames + rejeu tracker — toujours pas fait, pas bloquant pour V1 comme prévu. **Dépendance : retour d'usage sur V1.** **Rejoint le même besoin technique que le "pause/reprise + rewind" de la Vue live GUI (`docs/gui/spec.md` § 1.5bis, ajouté 2026-08-10)** — même mécanisme (tampon glissant de frames par flux), deux usages différents (rattraper le tracker vs laisser l'utilisateur revenir en arrière) — à concevoir ensemble le jour où l'un des deux est fait. **Recoupe § H1 "Pause/reprise + buffer de rewind"** — un seul chantier, suivi d'exécution côté § H1.
- [x] **Concurrence ajoutée en même temps, non planifiée séparément dans cette TODO à l'origine mais nécessaire** : `trackManager` (`tracking.go`) devient partagé entre les deux boucles — `sync.Mutex` ajouté, verrouillage grossier (toute la map + les appels tracker sous le même verrou, les instances `tracker.ObjectTracker` natives ne supportent de toute façon pas l'accès concurrent). Le vrai gain n'est pas le parallélisme (aucun — gocv tourne intentionnellement en 1 thread, `SetNumThreads(1)`) mais que l'appel YOLO (`AnalyzeFrame`, le coût dominant) tourne **hors verrou** — la boucle vidéo n'attend jamais dessus, seulement le post-traitement (association IoU, mutation de tracks), bien plus rapide. Mutex défensif ajouté aussi dans `gocv-tracker` (cache Mat partagé) en profondeur de défense, même si le verrou de `trackManager` suffit déjà en théorie. **Pas testé avec `-race` en conditions réelles** (webcam nécessaire) — compilé et testé unitairement avec `-race`, aucune race détectée sur ce qui est exerçable sans caméra.

## A — Cascade YOLO → crop → CLIP

Référence matrice : A — cascade et CLIP ABSENTS. Segmentation volontairement non portée (décision ci-dessous) — le vrai travail restant est CLIP.

- [x] Récupérer `stash@{0}` — **fait le 2026-08-06, mais pas comme prévu** : le stash lui-même avait déjà disparu (`git stash list` vide, reflog absent) au moment de reprendre ce chantier, seul le commit orphelin (`fb02e91`, objet non garbage-collecté) subsistait. Récupéré sur `recover/yoloe11s-seg-stash`. Contient : `yoloe11s-seg` complet (détection + segmentation), `AI.AnalyzeFrame(frame, filters)` avec filtrage par classe, `Render()` renvoyant `(bool, error)`.
- [x] **Décision 2026-08-10 : segmentation `yoloe11s-seg` volontairement pas portée.** Inspecté : le code du stash utilise `github.com/deadelus/go-clean-onnxruntime`, entièrement supprimé du projet au commit `4cad31d` (remplacé par `onnxruntime_go` en binding direct) — ce n'est pas un portage copier-coller, ce serait une réécriture complète au même effort qu'écrire le module de zéro. Vu que la TODO elle-même classe la segmentation comme optionnelle ("seulement utile sur scènes très encombrées", pas le comportement par défaut) et que ça n'apporte rien à la cascade CLIP (qui n'a besoin que de YOLO détection + crop, déjà là), le travail est reporté indéfiniment plutôt que fait maintenant pour du confort. `recover/yoloe11s-seg-stash` reste disponible si un besoin réel se présente.
- [x] **Vérifié en conditions réelles le 2026-08-10** (`docs/adr/clip-backend.md` § 6) : poids quantized téléchargés (`assets/models/clip/`, 89+64.5 Mo), `EncodeText`/`EncodeImage` tournent contre les vrais poids. Corrections trouvées au premier run : `text_model.onnx` n'a pas d'`attention_mask` (juste `input_ids`, corrigé) ; même crash SIGABRT que le bug critique déjà résolu pour YOLO, `clip.Encoder.Cleanup()` n'appelait pas `runtime.DestroyEnvironment()` (corrigé, 3/3 sorties propres après). `EncodeImage` testé sur une frame non recadrée (test rapide) donne le bon ordre de similarité mais des marges faibles — attendu, le crop YOLO (item suivant) devrait nettement améliorer ça.
- [x] Crop de la frame sur la bbox YOLO avant envoi à CLIP (pas la frame entière) — **fait le 2026-08-10** (`fb8dbd1`). `reanchor()` fait `frame.Crop(box)` puis `EncodeImage` sur le crop, pour chaque détection YOLO à chaque passe de re-détection.
- [x] Calcul des embeddings texte des filtres une seule fois — **fait le 2026-08-10** (`fb8dbd1`), dans `newTrackManager()` (une fois par appel `RecognitionUseCase`, pas par frame ni par détection).
- [x] **Décision 2026-08-10 (option a) : CLIP remplace entièrement le matching par label COCO**, pas un simple fallback. `reanchor()` : YOLO ne fait plus que proposer des régions candidates (`AnalyzeFrame` sans filtre), c'est la similarité cosinus crop↔texte via CLIP qui décide seul si l'objet correspond au filtre (`box.Label != filter` supprimé). `bestMatch()` (association IoU inter-frame par label COCO, `internal/application/uc/tracking.go`) est inchangé — usage orthogonal, bookkeeping spatial, pas décision sémantique.
- [x] **Testé end-to-end le 2026-08-10** (`livesemantic recognition --filter person`, webcam réelle) : le gate sémantique fonctionne — track "person" confirmé en continu sur ~100 passes reanchor. Bug pré-existant trouvé et corrigé au passage (`6f634e1`) : `pflag.Parse()` global bloquait toute sous-commande cobra avec flags (`unknown flag: --filter`, exit(2) avant même d'atteindre `RecognitionUseCase`) — c'est ce qui empêchait ce test d'être fait plus tôt.
- [x] **Calibration du seuil — investiguée et défaut corrigé le 2026-08-10** (`docs/adr/clip-backend.md` § 7). Défaut CLI/survey changé `0.8` → `0.25`. Root cause confirmée via un outil de debug jetable + une image de référence propre (COCO, hors webcam) : le pipeline (crop, prétraitement, encodage) n'est **pas** buggé — classement correct sur image propre ("cat" gagne bien, marge réelle mais faible ~0.012). La confusion observée sur crops webcam ("couch" > "person" dans certains cas) vient de bbox YOLO peu précises (beaucoup de fond dans la boîte) + frames basse résolution, qui compriment encore plus les marges déjà faibles du zero-shot CLIP ViT-B/32 (écart gagnant/second de l'ordre de 0.01-0.03 même sur image propre).
- [ ] **Seuil absolu intrinsèquement fragile — piste non explorée.** `0.25` (puis `0.20`, voir item suivant) est un point de départ défendable, pas une valeur validée pour tous les cas. Piste plus robuste esquissée dans l'ADR § 7 : score relatif contre un prompt "négatif" générique plutôt qu'un seuil absolu (pratique CLIP standard = softmax sur un ensemble de classes, pas de seuil isolé). Pas fait.
- [x] **Défaut abaissé `0.25` → `0.20` le 2026-08-11**, suite à un run réel bloqué signalé par l'utilisateur (`livesemantic -i`, webcam, `active_tracks: 0` sur toute la session). **D'abord suspecté à tort comme régression de la migration `DynamicAdvancedSession` de la veille** (§ H1) — écarté par une vraie vérification, pas une supposition : mêmes crops rejoués sur le commit pré-migration, résultat identique aux 4 décimales (0.2351/0.2381). Root cause réelle : § 7 se confirme même sur vidéo de test propre (`person.mp4`, pas juste crops webcam dégradés) — un vrai match "person" scorait 0.235-0.238, sous l'ancien défaut 0.25. `cli_recognition.go` + `cmd_realtime_analysis.go` mis à jour. Détail complet : `docs/adr/clip-backend.md` § 10. Le point ci-dessus (seuil relatif) reste la vraie solution long terme, non résolu par ce changement de valeur.
- [x] **Bbox YOLO trop larges — piste "inset systématique" testée le 2026-08-10 et écartée (résultat réel, pas supposé).** Outil jetable (`cmd/crop-inset-test`, supprimé après usage) : capture webcam réelle, A/B recadrage bbox brute vs bbox rétrécie (10/20/30% de marge par côté), 8 passes, 9 échantillons "person" + 5 "couch". **Résultat net et cohérent, mais dépendant de la classe** : rétrécir améliore "person" (8-9/9 échantillons positifs à 20-30%, +0.010 à +0.014 en moyenne — la dilution par le fond diminue, comme prévu) mais dégrade systématiquement "couch" (5/5 négatifs, de plus en plus fort avec la marge, -0.015 à -0.029 en moyenne — un canapé perd son silhouette/ses bords identifiants en étant rogné, contrairement à une personne). **Pas de fix généralisable sans connaître la classe** — et conditionner le resserrement sur le label COCO réintroduirait une dépendance à la classification fermée que la décision (a) visait justement à éliminer. Écarté sans implémenter : un inset universel améliorerait certains filtres et en dégraderait d'autres silencieusement, pire qu'un statu quo documenté. Détails dans `docs/adr/clip-backend.md` § 9.
- [x] Score de similarité CLIP threadé dans `TrackEvent` — **fait le 2026-08-10**. `TrackEvent.Score` (nouveau champ, `entities/track.go`), rempli par `MatchDetection(box, at, score)` (signature étendue). `reanchor()` (`tracking.go`) conserve le score déjà calculé pour le gate sémantique au lieu de le jeter après le check de seuil, et `emit()` notifie avec `evt.Score` au lieu de `box.Confidence` (YOLO). Test dédié `TestMatchDetectionThreadsScoreOntoEvent` (`track_test.go`).
- [x] **Benchmark du coût CLIP par détection — fait le 2026-08-10** (`docs/adr/clip-backend.md` § 8, outil jetable `cmd/perf-bench`, supprimé après usage, image COCO fixe 640x480/3 boîtes hors webcam pour un résultat reproductible, CPU, poids quantized, n=30 par mesure). `YOLO AnalyzeFrame` ~182ms (154-260ms) ; `Frame.Crop` ~9µs (négligeable, confirmé zero-copy) ; `CLIP EncodeImage` ~46ms/boîte (36-68ms) ; `CLIP EncodeText` ~23ms (payé une fois par filtre au démarrage, pas sur le chemin chaud). Estimation `reanchor()` à 3 boîtes : YOLO(182ms) + 3×46ms ≈ 320ms — cohérent avec les 230-390ms observés en webcam réelle (§ ci-dessus).
- [x] **Piste écartée 2026-08-10 : spécialiser/alléger YOLO (yolo11n, ou un modèle mono-catégorie) maintenant que CLIP décide.** Rejetée sans l'implémenter, pour deux raisons distinctes : (1) un YOLO mono-catégorie casserait le filtre texte libre lui-même — YOLO doit proposer des boîtes toutes classes confondues, c'est CLIP qui filtre après, pas avant ; (2) le calibrage § ci-dessus montre que le vrai goulot est la **précision des bbox** (trop de fond dilue le signal CLIP), pas la classification COCO — yolo11n est généralement moins précis en localisation que yolo11s, risque d'aggraver le problème identifié plutôt que de le résoudre. **Correction 2026-08-10 (benchmark ci-dessous) : l'affirmation initiale "YOLO n'est pas le poste dominant" était fausse/pas mesurée — en réalité `AnalyzeFrame` seul (~182ms) domine largement un seul `EncodeImage` CLIP (~46ms), et n'est dépassé par le coût CLIP cumulé qu'à partir de ~4 boîtes candidates par frame.** Ça renforce quand même la conclusion (YOLO reste le vrai poste de latence, pas CLIP) sans changer la décision de ne pas alléger YOLO (le risque bbox reste le même). Pas fait.
- [x] **Décision 2026-08-11 : le filtrage par label COCO remplace le gate sémantique CLIP — inverse la décision (option a) du 2026-08-10 ci-dessus.** Demandé explicitement par l'utilisateur après le run bloqué (item précédent) : le seuil absolu CLIP restait fragile même à 0.20 (confondants type "couch"/"plante" scorant dans la même plage que de vrais "person", cf. `docs/adr/clip-backend.md` § 7 — un crop "couch" avait déjà scoré 0.2476 pour "person", au-dessus de 0.20 **et** de l'ancien 0.25). `reanchor()` (`internal/application/uc/tracking.go`) : le candidat est accepté si et seulement si `box.Label` (YOLO) figure dans le filtre demandé — plus d'appel CLIP du tout sur ce chemin. Nouvelle syntaxe de filtre (`internal/application/uc/filter_spec.go`, `parseFilterSpec`) : `label` (implicite = plafond 1), `label*N` (plafond N), `label*N,label2` (plusieurs labels indépendants — **doublons rejetés à l'analyse**, pas de chevauchement possible entre termes, cf. item suivant). Chaque label validé contre les 80 classes COCO à l'analyse (erreur claire sur typo, pas un silence). `dto.RecognitionRequest.SimilarityThreshold` supprimé (plus utilisé) — CLI (`cli_recognition.go`), cmd (`cmd_realtime_analysis.go`), API (`recognition.go`) et tests mis à jour en conséquence. **Conséquence assumée, pas un oubli** : plus de filtre texte libre hors vocabulaire COCO (ex. "sac abandonné" redevient impossible à filtrer par ce chemin) — l'infra CLIP (`clip.Encoder`) reste chargée/wired (`main.go`, `NewUseCase`) mais inutilisée pour le matching ; gardée pour la galerie de références par image (§ D, `EncodeImage` seul). **Testé en conditions réelles** (webcam, `--web`, filtre `person*2`) : track "person" confirmé en continu (`state: Confirmed`), `active_tracks` ne reste plus jamais à 0 — contrairement à l'ancien gate CLIP sur la même scène. Root-cause du run initial confirmée a posteriori : pas juste un seuil mal calé, le principe même d'un score absolu était le problème pour ce cas d'usage. Détail complet : `docs/adr/clip-backend.md` § 12.
- [x] **Fail-fast ajouté au passage (2026-08-11) :** `RecognitionUseCase` (`uc_recognition.go`) validait le filtre *après* avoir ouvert la caméra/fenêtre — trouvé en testant le rejet d'un filtre invalide via l'API (`unicorn`, pas une classe COCO) : la webcam s'ouvrait ~800ms puis se refermait aussitôt, pure perte + flash confus côté fenêtre CLI. `newTrackManager` (donc `parseFilterSpec`) tourne maintenant avant `streamingInput.Initialize()`/`streamingOutput.Initialize()`.
- [ ] **Limite connue, pas corrigée : l'API REST ne remonte pas au client un filtre invalide.** `POST /recognition/start` répond `202 Accepted` immédiatement (design async assumé, § H1) même si le filtre est invalide — l'échec n'apparaît que dans les logs serveur (`"Recognition session ended with error"`) et indirectement via `GET /recognition/status` qui repasse à `running:false` très vite. Pas grave pour un usage CLI (l'erreur remonte au terminal), plus gênant pour une future GUI qui devra soit poller intelligemment soit avoir un champ d'erreur exposé — pas conçu, à faire quand la GUI (§ H) en aura besoin.
- [x] **Décision 2026-08-11 (même après-midi) : le filtrage par label redevient hybride — CLIP n'est pas supprimé, il redevient un second mécanisme, pas le seul.** Demandé explicitement par l'utilisateur : CLIP "était utile quand même", à garder pour la GUI, avec une valeur par défaut fixe pour l'instant (pas de prompt CLI). `reanchor()` (`internal/application/uc/tracking.go`) tourne désormais en deux passes : **Pass 1** — un terme qui est une des 80 classes COCO matche exactement sur `box.Label` (inchangé vs l'item précédent, aucun appel CLIP). **Pass 2** — un terme qui n'est *pas* une classe COCO (ex. `"person with a red hat"`) est traité comme sémantique : CLIP score chaque candidat **non déjà réclamé par la pass 1**, les candidats sous `defaultSimilarityThreshold` (constante cachée, `0.20`, pas exposée CLI/API — but affiché : devenir un contrôle GUI, § H) sont écartés, les survivants sont **classés par score** et seuls les `N` meilleurs (le plafond du terme) sont retenus — le plafond devient donc un vrai "top-N" pour un terme sémantique, pas juste premier-arrivé. `filterTerm.Label` renommé `Key` (peut être un label ou du texte libre) ; `parseFilterSpec` **n'erreure plus sur un terme hors COCO** (c'est désormais un terme sémantique valide, pas un typo) ; nouveau `termMatch{Cap, Embedding}` (`Embedding` nil = exact, non-nil = sémantique) ; `trackedObject.filterKey` (le `Key` du terme qui a fait naître ce track, pas forcément égal à `track.Class`) remplace le décompte par `countByLabel` → `countByFilterKey`. **Comportement par défaut confirmé le 2026-08-11 (pas un bug — reformulé depuis une version précédente de cette entrée qui le présentait comme une limite à corriger)** : si un terme exact et un terme sémantique visent le même label YOLO (ex. `person*1,person with a yellow hat*1`, tous les deux `person`-labellisés côté YOLO), la pass 1 réclame *toutes* les boxes de ce label avant même que la pass 2 les voie — une box du terme exact ne "retombe" jamais vers le terme sémantique. Testé en réel (webcam, seul devant la caméra) : sans ça, le terme sémantique aurait pu dessiner une 2e box sur la même personne. **L'utilisateur ne veut pas ce chevauchement par défaut** — un paramètre nommé (`overlap`, pas construit) pourrait l'activer explicitement plus tard si besoin, voir item ci-dessous. **Testé en conditions réelles** (webcam, `--web`) : `person*1,person with a red hat*1` — le terme exact ne matche rien (pas de personne dans le cadre au moment du test), le terme sémantique matche... une plante en pot (`class: potted plant`, `state: Confirmed` en continu, plafond=1 respecté) — **confirme mécaniquement que le pipeline hybride fonctionne** (partition exact/sémantique, classement par score, plafond) **et confirme aussi que la fragilité CLIP documentée (§ 7/§ 10) est toujours bien réelle**, maintenant sur le chemin sémantique plutôt que sur un ancien gate global. Détail complet : `docs/adr/clip-backend.md` § 13.
- [ ] **Nouveau : fonction d'auto-calibration du seuil sémantique.** Demandé par l'utilisateur (2026-08-11) : les actions (item suivant, pas construites) pourraient ajuster `defaultSimilarityThreshold` automatiquement — trop de "bruit" (trop de matches, comme la plante ci-dessus) → remonter le seuil ; pas assez → le redescendre. Rien de conçu : ni la métrique de "bruit" (taux de faux positifs ? nombre de candidats au-dessus du seuil par cycle ? autre ?), ni la fenêtre temporelle d'observation, ni si l'ajustement est par terme ou global. Dépend du système d'événements/actions (item suivant) pour avoir un point d'accroche.
- [ ] **Nouveau (§ I à créer si ce chantier démarre) : système d'événements/actions déclenchés par une condition de scène.** Le `*N` du filtre (`person*2`) est actuellement un plafond pur (exact) ou un top-N par score (sémantique) — l'intention à terme (demandé par l'utilisateur 2026-08-11) est qu'une condition de scène (ex. "2 personnes ou plus simultanément", ou "trop de bruit sur ce terme") puisse **déclencher une ou plusieurs actions/événements** configurables, dont l'auto-calibration ci-dessus. Rien de conçu : ni le modèle d'action (notifier existant ? webhook ? autre ?), ni où la condition s'évalue (probablement `trackManager`, elle a déjà tout le décompte par terme via `countByFilterKey`), ni la syntaxe pour associer une action à un terme de filtre. Prérequis avant de commencer : décider ce à quoi une "action" ressemble.
- [x] **Syntaxe `+option` pour le paramètre `overlap` — tranchée et parsée le 2026-08-11 soir, pas encore branchée dans `reanchor()`.** Grammaire décidée avec l'utilisateur : `key[*cap][+option[=valeur]]...` — `+overlap` seul = `true` implicite, `+overlap=false`/`+overlap=true` explicite, chaînable pour de futures options (`+opt1+opt2=val`). `!overlap` (première proposition) écarté : ambigu, se lit plus naturellement comme une négation dans plein de conventions (shell, comparaisons) que comme une activation. `filterTerm.Overlap` (`filter_spec.go`) et `termMatch.Overlap` (`tracking.go`) portent la valeur parsée — **`reanchor()` ne la consulte encore nulle part**, le comportement reste `overlap=false` partout quoi qu'on mette dans le filtre (juste stocké, prêt pour le câblage réel). Grammaire d'option volontairement stricte (contrairement au `key` qui accepte n'importe quel texte libre depuis la décision hybride) : nom d'option inconnu, valeur non-booléenne, ou option répétée dans un même terme → erreur claire, pas de silence. 11 nouveaux cas de test (`filter_spec_test.go`). Défaut `overlap=false` confirmé cohérent CLI/API/GUI (`docs/gui/spec.md` § 3.3, case à cocher par terme décochée par défaut). Détail : `docs/adr/clip-backend.md` § 16.
- [x] **`Overlap` câblé dans `reanchor()` — fait le 2026-08-11 soir**, avec un second correctif : **restriction sémantique par label mentionné** (`semanticLabelHint`, `filter_spec.go`) pour éliminer le bruit couch/plant signalé par l'utilisateur. Pass 2 : `if claimed[i] && !term.Overlap { continue }` (une box déjà réclamée reste évaluable si le terme courant a `Overlap=true`) puis `if term.LabelHint != "" && box.Label != term.LabelHint { continue }` — si le texte libre du terme mentionne exactement une classe COCO (ex. "person" dans "person with a yellow hat"), seules les boxes déjà labellisées ainsi par YOLO sont candidates ; 0 ou 2+ classes mentionnées = pas de restriction (comportement inchangé). **Bug plus profond trouvé en écrivant le test d'intégration** (pas spécifique à `overlap`, touchait *tout* spawn depuis le début de § D) : `spawn()` n'ajoutait jamais le nouveau track à `matchedTrackIDs` — conséquence 1, un second terme dans le même cycle pouvait "voler" un track tout juste spawné via `bestMatch` au lieu d'en créer un second ; conséquence 2 (plus grave, indépendante d'`overlap`), `missUnmatched` appelait `Miss()` sur un track dans le **même cycle** que sa naissance, remettant son compteur de hits à 0 — retardait silencieusement la confirmation de **tout** track d'un cycle supplémentaire, et pouvait faire passer un track directement en `StateLost` sans jamais confirmer si un miss suivait de près. Corrigé : `spawn()` renvoie l'ID créé, `matchOrSpawn` l'ajoute à `matchedTrackIDs`. Test de régression dédié (`TestReanchor_SpawnedTrackConfirmsInExactlyMinHitsCycles`) — **vérifié qu'il détecte bien le bug** en désactivant temporairement le fix avant de le restaurer, pas juste écrit et supposé correct. 4 nouveaux tests d'intégration (`overlap` par défaut/explicite, `LabelHint` qui ignore un confondant mieux noté). **Testé en conditions réelles** (webcam, filtre `person*1,person with a yellow hat*1+overlap`) : deux tracks simultanés (`track-1` classe `person`, `track-2` filterKey `person with a yellow hat`), tous les deux `Confirmed` en continu, scores sémantiques **jamais** couch/plant (toujours `yolo_label:"person"`, ~0.25-0.27). Détail complet : `docs/adr/clip-backend.md` § 17.
- [x] **Bug d'affichage trouvé juste après, en testant à la main (CLI) — corrigé le 2026-08-11 nuit.** L'utilisateur relance en CLI pour vérifier visuellement le double-box et n'en voit qu'un seul. Cause : `trackManager.boxes()` renvoyait `entities.BoundingBox` brut, dont `Label` est celui de **YOLO** ("person" pour les deux tracks, même détection physique) — le dessin (`uc_recognition.go`) utilisait ce label pour la couleur/le texte, donc les deux tracks se dessinaient avec la même couleur, le même texte, aux mêmes coordonnées : deux rectangles identiques superposés, indiscernables d'un seul, alors que les deux tracks existaient bien (déjà confirmé par les logs). Corrigé : `boxes()` renvoie maintenant `[]trackedBox{Box, FilterKey, TrackID}`, le dessin utilise `FilterKey` (le terme de filtre, distinct pour les deux tracks) plutôt que `Box.Label`. Nouveau test (`TestBoxes_DistinguishesTracksSharingTheSamePhysicalBox`). **Limite qui reste** : les deux boxes restent aux mêmes coordonnées exactes (factuellement correct, même objet physique) — seules couleur/texte les distinguent, pas de décalage visuel. Détail : `docs/adr/clip-backend.md` § 18.
- [x] **Décalage en cascade des boxes superposées — fait le 2026-08-11 nuit**, suite immédiate demandée par l'utilisateur. `cascadeOffsets` (`tracking.go`) : décale chaque box en diagonale de `cascadeStepPx` (16px) par rang, calculé sur des boxes en IoU mutuelle ≥ 0.85 (quasi-identiques seulement, pas deux objets différents qui se chevauchent un peu), classées par `TrackID` (pas l'ordre de la slice, qui vient d'une map Go à ordre randomisé — sinon le décalage scintillerait d'une frame à l'autre pour la même paire de tracks). 5 tests (`TestCascadeOffsets`). **Vérifié visuellement en conditions réelles** (capture d'une frame webcam via `/ws`, cette fois avec l'utilisateur bien dans le cadre) : deux rectangles décalés, rouge "person (86.35%)" et gris "person with a yellow hat (86.35%)", les deux textes lisibles simultanément. Détail complet : `docs/adr/clip-backend.md` § 19.
- [x] **Score affiché trompeur — corrigé le 2026-08-11 nuit.** L'utilisateur reteste sans casquette jaune et voit quand même 2 boxes, toutes deux à "85.37%" — attendu côté matching (fragilité CLIP déjà documentée § 7/§ 14, confirmée ici en conditions contrôlées puisque le terme exact "person" prouve qu'une personne est bien détectée) mais **le chiffre affiché était trompeur** : les deux boxes montraient `box.Confidence` (confiance YOLO générique, identique pour les deux tracks), pas le vrai score CLIP (~0.25-0.28, visible dans les logs) qui a décidé le match sémantique. Corrigé : `matchOrSpawn`/`spawn` prennent un paramètre `score`, stocké sur `trackedObject.lastScore`, exposé via `trackedBox.Score` (`boxes()`) ; l'affichage montre `"(score 0.XX)"` pour un terme sémantique (`Score != 0`) au lieu de `"(XX.XX%)"` (confiance YOLO, gardée pour les termes exacts qui n'ont pas de score CLIP). Test dédié (`TestBoxes_ScoreReflectsWhatDecidedTheMatch`). **Pas re-vérifié visuellement** (webcam vide au moment du fix) — à confirmer par l'utilisateur. Détail : `docs/adr/clip-backend.md` § 20.
- [x] **Bug racine trouvé sous l'item précédent, corrigé le 2026-08-11 nuit : `bestMatch` ignorait `filterKey`.** L'utilisateur reteste, capture d'écran à l'appui : les deux pourcentages (normalisés en `%` au passage, demande faite en même temps) étaient **échangés** — le terme exact affichait ~21% et le terme sémantique ~91%, l'inverse de l'attendu. Cause réelle : deux tracks avec la même `track.Class` YOLO ("person" pour les deux) mais un `filterKey` différent (cas `+overlap`) — `bestMatch` ne filtrait que par classe, jamais par terme, donc l'appel d'un terme pouvait réanchorer le track de l'**autre** terme (ordre de map Go randomisé) et lui écraser son `lastScore`. Le `filterKey` du track restait correct (jamais modifié après spawn), seul le score fuyait — d'où l'échange visible. Corrigé : `bestMatch(box, filterKey, taken)` exige maintenant `obj.filterKey == filterKey` en plus de la classe. **Pourquoi pas attrapé avant** : tous les tests `+overlap` précédents n'appelaient `reanchor()` qu'une fois — au premier cycle `m.active` est vide, donc les deux termes passent par `spawn()`, jamais par `bestMatch()` ; le bug n'apparaît qu'à partir du 2e cycle. Nouveau test multi-cycles (`TestReanchor_OverlapTracks_ScoreStaysWithCorrectTrackAcrossCycles`) — **vérifié qu'il détecte le bug** : sans le fix, 20/20 runs en échec (quasi systématique, pas juste flaky) ; avec le fix, 20/20 au vert. **Vérifié visuellement en conditions réelles** : `person (89.97%)` / `person with a yellow hat (22.63%)` — valeurs enfin cohérentes avec la réalité (pas de casquette jaune → score sémantique bas). Détail complet : `docs/adr/clip-backend.md` § 21.
- [x] **Confirmé par un test contrôlé réel le 2026-08-11 : CLIP ne discrimine pas l'attribut couleur.** L'utilisateur teste `person with a yellow hat` avec une casquette noire puis une casquette jaune, même personne, même cadrage — **score quasi identique dans les deux cas, la box apparaît à chaque fois.** Confirme précisément l'hypothèse de l'item précédent (§ 14 : défaut "bag-of-words cross-modal" documenté dans la recherche, pas juste "CLIP est fragile" en général) — CLIP détecte "person"/"hat" comme concepts mais ne lie pas la couleur à l'objet. **Implication produit, pas tranchée** : le filtre texte libre par attribut de couleur (`"a yellow X"`, `"a red Y"`) n'est pas fiable avec ce modèle — les filtres sur un objet/concept entier (hors couleur) restent la vraie proposition de valeur. Options non tranchées : accepter et documenter la limite côté produit/GUI ; modèle CLIP plus grand (ViT-L/14, pas de garantie — le défaut de binding persiste sur des modèles plus grands dans la littérature) ; classifieur de couleur dédié en aval du crop YOLO (changement d'architecture) ; ne rien faire de plus pour l'instant. Détail complet : `docs/adr/clip-backend.md` § 22.
- [ ] **Décision 2026-08-11 : pas d'OWL-ViT/GroundingDINO — piste retenue pour "hat" (et attachements similaires) = fine-tuning YOLO, pipeline versionné dans le repo, pas un notebook externe jetable.** Ce runtime Go (ONNX Runtime + gocv) est un runtime d'**inférence**, pas d'entraînement (pas d'autograd) — le fine-tuning se ferait donc via un pipeline externe (Python/Ultralytics) mais **maintenu dans ce repo** (emplacement pas encore choisi, ex. `training/`), produisant un nouveau `.onnx` drop-in-compatible avec l'adaptateur existant (même famille YOLO11, juste poids/liste de classes différents) — la table de classes côté Go devra suivre les nouveaux indices de sortie. Portée volontairement limitée aux **objets** (ex. "hat", "backpack", "glasses" — accessoires connus à l'avance), pas aux couleurs : une couleur n'est pas un objet à encadrer, des classes du type "hat-yellow"/"hat-red" exploseraient combinatoirement (N objets × M couleurs à annoter) — la couleur reste une analyse de pixels en aval (histogramme HSV sur la box détectée), pas une classe YOLO. **Synergie notée** : une classe "hat" fine-tunée donne gratuitement une box précise pour l'histogramme couleur — plus besoin d'heuristique de sous-région ("tiers supérieur de la box person") ni d'un modèle lourd (OWL-ViT/GroundingDINO) pour la localiser. Rien de conçu/codé : ni le pipeline d'entraînement (dataset — piste Open Images V7, classe "Hat" déjà annotée avec bbox, à valider — puis fine-tuning Ultralytics depuis le checkpoint YOLO11s COCO, export ONNX même opset que le runtime actuel), ni la table de classes étendue côté Go, ni la liste des attachements à couvrir en premier, ni le module d'histogramme couleur lui-même. **OWL-ViT/GroundingDINO explicitement écartés comme piste principale** (vocabulaire ouvert par texte, zéro entraînement requis, mais lents — centaines de ms à qq secondes/image, backbone contrastif de la même famille que CLIP donc pas de garantie qu'ils lient mieux couleur/objet) — gardés en tête comme filet pour de l'imprévisible non anticipé seulement, pas comme solution pour "hat".
- [ ] **Rôle de CLIP après cette décision — clarifié le 2026-08-11 en discussion, pas encore acté en code.** CLIP ne fait **jamais** le tracking frame-à-frame (ça reste KCF/CSRT, port `tracking.ObjectTracker`, indépendant de CLIP) — son rôle actuel est de **scorer/décider** si une box déjà trouvée par YOLO correspond à un terme de filtre texte libre hors vocabulaire COCO (pass 2 de `reanchor`), une décision qui alimente ensuite le cycle de vie du track (spawn/confirm/match/miss) mais qui n'est pas elle-même du tracking. Une fois le fine-tuning YOLO (item ci-dessus) et l'histogramme couleur en place pour les cas anticipés (accessoires connus + couleurs), le périmètre réellement utile de CLIP se réduit aux **concepts sémantiques génuinement ouverts, non réductibles à une classe YOLO ni à un attribut de couleur** (ex. actions/états — "quelqu'un qui court", "porte ouverte" — ou tout concept pas encore fine-tuné) : il reste la voie de repli à vocabulaire ouvert, moins précise mais disponible immédiatement sans cycle d'entraînement.
- [x] **Corrigé le 2026-08-12 (ADR § 23-24) : scoring différentiel implémenté — le bug "2 boîtes avec ou sans hat" est réglé.** `termMatch.BaseEmbedding` (nouveau, `tracking.go`) — pour un terme sémantique avec un `LabelHint`, `newTrackManager` encode aussi le nom de base seul ("person") en plus du terme composé. `reanchor` pass 2 : `accepted := score >= defaultSimilarityThreshold && (score - baseScore) >= defaultDifferentialMargin` (nouvelle constante, `0.02`, valeur de départ non calibrée sur du réel — même réserve que `defaultSimilarityThreshold`) — ET logique, pas un remplacement : un score absolu faible reste rejeté même si le delta paraît grand. Un terme sans `LabelHint` (concept ouvert, pas de nom de base) garde l'ancien comportement au seuil absolu seul, inchangé. Log `"Semantic candidate scored"` étendu avec `base_score`/`delta`. 2 nouveaux tests (`TestReanchor_SemanticTerm_DifferentialMarginRejectsBareBaseNoun`, `...AcceptsRealSignal`) — **vérifié que le premier détecte bien le bug** : fix temporairement désactivé, 5/5 runs en échec ; fix restauré, tous verts. 7 tests existants ont dû être mis à jour (`mockSemanticEncoder` étendu à des embeddings 3D avec un axe dédié pour le nom de base, `baseAxisTexts`/`baseScoreByCropSize`) — sans ce changement de mock, tout terme composé mentionnant un mot COCO (quasiment tous les tests existants) aurait un delta nul par construction et rejeté à tort ; documenté en détail dans le commentaire du mock. `go vet`/`gofmt`/`go test -race` propres. **Confirmé en conditions réelles le 2026-08-12** (webcam, `logs/livesemantic.log`, filtre `person*1,person with a hat*1+overlap`, sans hat) : ~20 candidats "person with a hat" consécutifs, `delta` oscillant entre -0.005 et +0.014 (jamais ≥ `0.02`), tous `above_threshold: false` malgré des scores absolus (0.22-0.24) qui auraient clairé l'ancien seuil seul (0.20) — `active_tracks` reste à 1 en continu (le terme exact "person" seul), plus de 2e box fantôme. Le bug est bien réglé. **Cas positif confirmé juste après, même session, avec un vrai hat** : delta 0.02-0.038, `above_threshold: true`, `active_tracks: 2` en continu (les 2 boîtes coexistent bien avec `+overlap`) — le mécanisme distingue bien les deux cas. Nuance notée : le delta avec hat oscille assez près de la marge (parfois sous 0.02 selon l'angle/l'éclairage, track maintenu par la tolérance `StateCoasting`) — `0.02` fonctionne mais reste une marge serrée, pas recalibrée pour l'instant. Détail complet : `docs/adr/clip-backend.md` § 23-25.
- [ ] **Généralisation confirmée le 2026-08-12 (ADR § 23) : CLIP ne discrimine pas non plus la simple présence d'un accessoire, pas seulement sa couleur (§ 22).** Test réel : `person*1,person with a hat*1+overlap` → 2 boîtes en continu, avec ou sans casquette. Root-cause chiffrée : seuil `0.20` sous le score d'un "person" nu déjà mesuré à `~0.235-0.238` (§ 7/§ 10) — la clause "with a hat" ne fait pas assez redescendre le score en son absence, `LabelHint` restreint de plus le candidat sémantique à la **même boîte** que le terme exact. Piste d'amélioration non implémentée, non testée : **scoring différentiel** — `Δ = sim(crop, "person with a hat") − sim(crop, "person")`, accepter seulement si `Δ` dépasse une marge positive, au lieu d'un seuil absolu sur la phrase composée. Coût : un `EncodeText` de plus par terme composé, aucun `EncodeImage` supplémentaire. À essayer avant de conclure plus largement sur l'utilité de CLIP pour ce type de requête. Bilan honnête à date : périmètre utile de CLIP réduit à (1) concepts de scène non réductibles à objet+attribut+géométrie *si* le scoring différentiel fonctionne (hypothèse, pas testée), (2) recherche par image de référence (§ D, hors du problème de binding texte), (3) filet de repli pour la longue traîne non fine-tunée, imprécision assumée.
- [ ] **Vision cible consolidée le 2026-08-12 (ADR § 24) : décomposition géométrique + grammaire relationnelle extensible, en remplacement/complément des pistes CLIP pures.** Conclusion de fond, établie par élimination dans la discussion : **CLIP est structurellement dépendant d'une localisation préalable (YOLO ou équivalent) — sans elle, CLIP n'a rigoureusement rien à faire dans ce pipeline**, y compris pour la galerie de références (§ D, une sélection humaine unique ne fait apparaître aucun nouveau candidat sur un autre flux). Conséquence sur la priorité : le fine-tuning YOLO et la décomposition géométrique ci-dessous étendent ce que YOLO localise lui-même (fondamental), les pistes CLIP pures (scoring différentiel, image↔image, item précédent) ne réduisent jamais cette dépendance, seulement le taux d'erreur une fois qu'une box existe déjà — restent secondaires.
  - **Décomposition géométrique** : pour "X avec/portant Y" ou "X près de Y", décomposer en deux termes atomiques (chacun plus fiable seul qu'en phrase composée, § 14/23) reliés par une relation géométrique évaluée sur des boîtes déjà localisées, plutôt que scorer la phrase entière via CLIP. Si "hat" est fine-tuné (item ci-dessus) : deux boîtes YOLO indépendantes + primitives déjà existantes (`entities.BoundingBox.IoU`/`Intersection`/`Union`) — **zéro CLIP**. Sinon : repli sur une sous-région heuristique du conteneur (ex. tiers supérieur pour "hat") + CLIP scoré sur le terme **simple** ("hat" seul, pas la phrase composée) — strictement meilleur que ce qui a été testé jusqu'ici.
  - **Grammaire relationnelle extensible** (rien codé) : `term := key ['%' operateur ['=' valeur] '%' key] ['*' cap] ('+' option ['=' valeur])*` — un registre interne nom→évaluation, pas une liste fermée au niveau du tokenizer. Règle de non-ambiguïté tranchée : les suffixes `*cap`/`+option` s'appliquent toujours à la relation entière une fois consommée, jamais à l'opérande de droite seul (évite toute collision avec le `+` d'option déjà utilisé, § 16, sans désambiguïsation au cas par cas). Un seul opérateur retenu pour l'instant : `%+%` (containment). `%near=distance%` et les autres sont **volontairement différés** (chacun a ses propres paramètres, pas de valeur à les concevoir sans besoin réel).
  - **Cardinalité tranchée** (exemple "personnes près d'un sac", plusieurs sacs) : chaque paire (conteneur, attachement) valide est sa propre instance/match (3 sacs proches d'1 personne = 3 paires, pas 1) ; `*cap` borne le nombre de **paires** retenues, pas les instances de chaque côté indépendamment ; **appariement 1:1 glouton par défaut** (cohérent avec "pas de chevauchement par défaut", § 16), nouvelle option **`+shared`** (nom délibérément distinct de `+overlap` — axe différent, cardinalité intra-terme vs coexistence inter-termes) pour autoriser explicitement le N:M.
  - **Vision produit associée** (H2, pas backend, notée pour cohérence future seulement) : champ texte multi-termes avec surlignage live façon regex101 (3 familles visuellement distinctes : classe YOLO, terme custom de la galerie § D, texte libre CLIP), graphe interactif des modèles/termes connectés par flux — séquencé **après** que les briques réelles existent (fine-tuning + relations géométriques → galerie promue en termes nommables → grammaire v2 → surlignage → graphe), pas avant.
  - Prochaine étape suggérée, non commencée : spécifier `%+%` seul, bout en bout, avant `%near%`/`+shared`. Détail complet : `docs/adr/clip-backend.md` § 24.
- [x] **`%+%` (containment géométrique) implémenté le 2026-08-12, branche `feat/relational-operator`.** `filter_spec.go` : `splitRelation` extrait `container%relation[=param]%attachment` **avant** tout split sur `+` (nécessaire — `%+%` contient lui-même un `+`, le confondre avec le délimiteur d'option casserait le parsing) ; `relationOperators` (registre fermé, un seul nom pour l'instant : `+`) ; v1 exige que conteneur **et** attachement soient tous deux des classes COCO exactes (pas de repli CLIP/fine-tuning pour l'instant, cf. § 24) — erreur claire sinon. `tracking.go` : nouvelle **Pass 0** dans `reanchor` (avant les passes exact/sémantique existantes) — pour chaque terme relationnel, apparie les boîtes YOLO `Container`/`Attachment` via `containmentRatio` (fraction de la boîte attachement recouverte par le conteneur — **pas un IoU**, qui pénaliserait à tort un petit objet dans un grand, cf. commentaire) au-dessus de `relationContainmentThreshold` (`0.5`, non calibré, même réserve que les autres constantes de seuil) ; appariement glouton 1:1 par défaut (une boîte ne rejoint qu'une seule paire par cycle — `+shared` pour du N:M reste non implémenté) ; classé par ratio décroissant, top-`Cap` paires gardées ; la boîte **conteneur** devient l'entité suivie/affichée (`matchOrSpawn` avec le `Key` canonique complet comme `filterKey`), l'attachement sert seulement à la décision, pas suivi séparément (scope MVP). 8 nouveaux tests (`filter_spec_test.go` : grammaire — relation valide, opérateur inconnu, conteneur/attachement non-COCO, container==attachment rejeté, `%` non fermé, dédup par clé canonique ; `tracking_filter_test.go` : match/no-match, plafond, exclusivité gloutonne 1:1) — **vérifié que le test d'exclusivité gloutonne détecte bien une régression** (garde désactivée temporairement → échec reproductible ; restaurée → vert). `go vet`/`gofmt`/`go test -race` propres. **Testé en conditions réelles le 2026-08-12** (webcam, `person%+%backpack`). Premier essai : aucune box visible — diagnostiqué grâce au nouveau logging (item suivant) : `attachment_candidates: 0` en continu, **YOLO ne détectait jamais "backpack" du tout**, indépendant de la logique de containment. Isolé avec un filtre `backpack` seul : détection intermittente/instable confirmée (`active_tracks` clignote 0/1). Retest de `person%+%backpack` avec une pose plus stable : **plusieurs matchs réels confirmés** (`ratio: 1.0, 1.0, 0.976, 0.812`, tous `above_threshold: true`) — la logique de containment est donc validée avec de vrais chiffres. Explication du "je n'ai rien vu à l'écran" malgré ces matchs : `maxMissesBeforeLost=2` (réglé pour un tout autre cas, `entities/track.go`) tue le track après 2 ratés consécutifs — avec une détection "backpack" aussi instable, le track meurt presque aussitôt après chaque match, le temps d'affichage réel est de l'ordre d'un cycle, trop bref pour l'œil. **Pas un bug de `%+%`** — la même limitation de stabilité de détection YOLO déjà documentée plusieurs fois (§ 9/§ 10), pas retouchée ici (changer `maxMissesBeforeLost` affecterait tous les tracks, pas seulement ce cas). Détail complet : `docs/adr/clip-backend.md` § 26.
- [x] **`%near=distance%` (proximité géométrique) implémenté le 2026-08-12, branche `feat/near-relation-operator`.** Même grammaire que `%+%`, nouveau registre `relationOperatorSpec{requiresParam bool}` : `+` sans paramètre, `near` en exige un (positif, numérique — erreur claire sinon), parsé une fois en `float32` à `parseFilterSpec`. Métrique : `boxGap` (écart de bord à bord, pas centre-à-centre — une grande boîte adjacente à une autre ne doit pas être pénalisée par la distance de ses centres). Pass 0 généralisée : calcul + tri dépendent de `term.Relation` (`containmentRatio` décroissant pour `+`, `boxGap` croissant — le plus proche gagne — pour `near`). 3 nouveaux tests `reanchor` (match/rejet/classement — le classement utilise deux paires indépendantes, une seule ne suffit pas à le démontrer puisque seule la boîte conteneur est suivie) + 5 cas de grammaire. **Classement vérifié détecter une régression réelle** (tri désactivé temporairement, 5/5 échecs ; restauré, vert). **Testé en conditions réelles** : câblage confirmé réactif (`person%near=500%chair`, logs à chaque cycle), pas de scène avec un vrai match disponible pendant ce test automatisé — cas positif encore à valider visuellement par l'utilisateur. `go vet`/`gofmt`/`go test -race` propres. Détail complet : `docs/adr/clip-backend.md` § 27.
- [ ] **Nouveau (2026-08-12, différé) : revoir `maxMissesBeforeLost` (actuellement `2`, `entities/track.go`) — trop agressif pour un track dont la ré-acquisition dépend d'une détection YOLO intrinsèquement instable (ex. l'attachement d'un terme relationnel `%+%`, mais potentiellement tout autre cas similaire).** Constaté en testant `%+%` en réel (§ 26) : un track meurt après seulement 2 ratés consécutifs, ce qui tue quasi instantanément un match pourtant réel/valide quand la détection sous-jacente flickère (backpack détecté 1 cycle sur 4-5). Piste non tranchée : valeur plus tolérante globalement (impact sur tous les tracks, exact/sémantique/relationnel, pas juste ce cas) ? Tolérance différente par type de terme (relationnel vs exact/sémantique) ? Corrélée avec la fiabilité mesurée de la classe concernée ? Rien de conçu — à reprendre plus tard, pas urgent.
- [ ] **Question ouverte (posée par l'utilisateur 2026-08-11) : `box.Confidence` (YOLO, `decodeDetections`) et le score de similarité CLIP sont-ils normalisables sous un même concept ?** Réponse courte apportée dans la conversation, pas encore actée en code : **non, pas numériquement** — `box.Confidence` est une probabilité de classification parmi 80 classes entraînées (plage typique ~0.5-1.0, déjà seuillée à `thresholdConfidence=0.5` au décodage), la similarité cosinus CLIP mesure un angle entre deux embeddings dans un espace joint image/texte (plage réelle mesurée ~0.19-0.29, § 7) — deux grandeurs de nature différente, les fusionner numériquement (min-max, z-score) serait arbitraire, pas principié. **Ce qui est réellement unifiable : le concept UX, pas le calcul** — exposer un seuil de confiance par terme (quel qu'il soit, exact ou sémantique) sous une même interface utilisateur ("à quel point ce filtre doit être strict"), calculé différemment en interne selon le type de terme. Pas fait : `thresholdConfidence` (YOLO) reste une constante globale (`yolo11s.go`, 0.5, jamais par terme) alors que `defaultSimilarityThreshold` (CLIP) est déjà par-conception applicable par terme (juste pas encore rendu configurable par terme non plus). Piste pour plus tard si utile : un seuil par terme dans `filterTerm` (même champ, sémantique différente selon `isCOCOLabel`), pas juste une constante globale unique.

## Bugs UX ponctuels (indépendants du séquencement G→F)

- [x] Effet miroir sur le flux webcam — fait (`ae577b5`) : `gocv.Flip(imgMat, &imgMat, 1)` dans `camera.go` après `Read()`, avant `ToImage()`. Coché tardivement ici, l'implémentation datait d'avant cette relecture.

## F — Optimisations transverses (en continu, pas une phase isolée)

Référence matrice : F — PARTIEL, l'ordre de préférence ONNX-first est trivialement respecté (seul backend existant), le reste dépend de A.

- [x] Benchmarker réellement la latence CLIP une fois intégré (décision A) — **fait le 2026-08-10 sur CPU ARM (Apple Silicon, seul matériel disponible ici)** : voir § A ci-dessus et `docs/adr/clip-backend.md` § 8 pour le détail. x86 non testé — pas de matériel x86 disponible dans cette session, à refaire si besoin sur ce matériel.
- [x] Documenter l'ordre de fallback ONNX Go natif > Python embarqué > REST local > API cloud dans le code — **fait le 2026-08-10**, doc de package sur `internal/infrastructure/inference/detector.go` (référencée depuis `semantic_encoder.go`, dont le commentaire périmé "No implementation exists yet" a aussi été corrigé).
- [x] Corrigé le 2026-08-09 (partiel — voir item suivant, pas la cause principale) : `gocvtracker.Tracker.Init`/`Update` reconvertissait `image.Image → gocv.Mat` indépendamment pour chaque track actif au lieu de partager la conversion. Fix : cache par identité de pointeur `*entities.Frame` (`sharedFrameMat`). Conversion mesurée à 0.3-1.6ms en réalité (pas ~100ms comme estimé initialement) — gain réel mais pas la source du ralentissement observé.
- [x] **Cause racine trouvée le 2026-08-09 via `sample` (profiler CLI macOS, pas de Xcode/Instruments nécessaire), corrigée en deux temps.** D'abord OpenCL : `KCF/CSRT backend.Update()` passait ~330 échantillons sur 10s dans `TrackerKCFImpl::updateProjectionMatrix → oclTransposeMM → cv::ocl::Kernel::run → clFinish (OpenCL) → AppleIntelKBLGraphicsGLDriver → IOAccelContextFinish → mach_msg2_trap` — le T-API d'OpenCV (`UMat`/OpenCL) décharge une transposition de matrice sur le GPU intégré Intel, dont la synchro (`clFinish`) coûte des centaines de ms, plus cher que le calcul CPU direct (gotcha connu d'OpenCV T-API sur petites matrices/iGPU faible). Corrigé en dur (`os.Setenv("OPENCV_OPENCL_DEVICE", "disabled")` dans `gocv-tracker.init()`, plus besoin de variable d'env manuelle) — gain confirmé (~250-270ms → ~180-200ms) mais **incomplet**. Reprofilé après coup : le reste du temps est dans `denseGaussKernel`/`getSubWindow` (FFT/corrélation KCF elle-même, calcul CPU légitime), dont le coût scale avec la taille de la zone suivie — une personne proche de la webcam donne une bbox bien plus grande que dans les vidéos de test (`cmd/tracking-drift-bench`, 2-9ms). Corrigé par downscale avant tracking (`maxTrackingDimension=640`, pratique standard en CV temps réel — track sur un proxy réduit, remet le résultat à l'échelle). Rien à voir avec les threads ORT ni `VECLIB_MAXIMUM_THREADS` (testés et infirmés avant ces deux causes). **Confirmé en conditions réelles le 2026-08-09** (`logs/livesemantic.log`) : `detect_or_track_ms` tombe à 5-15ms en régime normal (identique à `cmd/tracking-drift-bench`), remonte à ~90-110ms quand le sujet se rapproche de la caméra (bbox plus grande) — 3-5× mieux que les 300-500ms d'avant dans tous les cas. La caméra (~10-15 FPS réel, `inter_frame_gap_ms`) est redevenue le facteur limitant, pas le tracker. Investigation perf close, sauf régression future.
- [x] Bug distinct trouvé pendant l'investigation (2026-08-09) : le filtre de classe (`req.Filter`) n'était appliqué qu'à l'alerte, jamais au tracking — tout objet détecté était suivi/dessiné (pré-existant, jamais un vrai filtre avant le rewrite § B). Corrigé (`trackManager.reanchor`). **Deuxième bug trouvé immédiatement après, à cause du premier fix** : le filtre n'était jamais trim/lowercase — une saisie CLI avec un espace parasite (`" person"` au lieu de `"person"`) filtrait silencieusement *toutes* les détections, sans erreur, symptôme identique à "la détection est cassée". `normalizeFilter` (trim + lowercase) ajouté à chaque point de comparaison (`reanchor`, `emit`) plutôt que de faire confiance à un seul point d'entrée.

## H — GUI web + desktop

Référence : Décision H (topologie de déploiement) tranchée le 2026-08-10 — un seul backend Go, deux façades UI (web + desktop via Wails). Spec complète : `docs/gui/spec.md` (technique/backend) et `docs/gui/design-brief.md` (produit/écrans, brief envoyé au design). Maquettes reçues le 2026-08-10 (`docs/gui/mockups/`, 8 écrans 1a-1h) — **pas encore implémentées**, cf. discussion. **Rien de cette section n'est commencé.** Ordre strict à l'intérieur de H : **H1 (backend) avant H2 (web) avant H3 (desktop)** — H2 câblée sur des endpoints/WS qui n'existent pas encore serait du travail jetable (mock à refaire).

### H1 — Prérequis backend (priorité actuelle, bloquant pour H2/H3)

Ordonné par dépendance, reprend `docs/gui/spec.md` § 1 et § 5 sans le redupliquer en détail (voir le fichier pour le raisonnement/les risques).

- [x] **Câblage API minimal — fait le 2026-08-10.** Fusion des deux serveurs gin auparavant indépendants et non branchés (`transport/adapters/api` + `transport/adapters/websocket`, chacun son propre port) en **un seul serveur** (`internal/transport/adapters/api`, flag `-s`/`--web` unique, l'ancien `-w`/`--websocket` supprimé) — nécessaire pour que REST (contrôle) et WS (vidéo) partagent la même session, ce que deux processus séparés ne permettaient pas. `POST /api/v1/recognition/{start,stop}` + `GET /api/v1/recognition/status` (via `handlers.BaseHandler.HandleRecognitionUseCase` existant, enfin utilisé) ; `GET /ws` diffuse les frames JPEG annotées (boxes déjà dessinées par `RecognitionUseCase`, pas de JSON séparé — écart assumé vs le format cible `docs/gui/spec.md` § 2, suffisant pour valider le transport). Nouvel adaptateur `implementation/streamer/output.WebSocketOutput` (`OutputStream` sur des clients WS au lieu d'une fenêtre gocv) + `uc.UseCases.Stop()` (nouveau, délègue à `streamingInput.Stop()`) pour arrêter une session depuis la route REST. **Testé bout-en-bout réel** (pas juste compilé) : webcam locale démarrée/arrêtée via curl, ~4 Mo de frames JPEG reçues sur `/ws` en 6s avec un client WS brut, double `stop` renvoie 409, arrêt propre confirmé dans les logs (pas de SIGABRT). **Limite assumée, pas un oubli** : une seule session pour tout le process (`streamingInput`/`streamingOutput` sont des champs partagés de `UseCase`, pas paramétrés par appel) — un second `start` pendant qu'une session tourne renvoie 409 plutôt que de corrompre l'état. Le multi-flux (item suivant) remplacera ce garde-fou par de vraies sessions adressables par ID. **Tests automatisés ajoutés le 2026-08-11** (`websocket_output_test.go`, 9 cas ; `recognition_test.go`, 6 cas table-driven avec `uc.UseCases` mocké) : broadcast/drop-client-mort/Cleanup côté `WebSocketOutput` (connexions WS réelles via `httptest.Server`, pas de mock gorilla) ; start/stop/status/409-si-déjà-lancé/400-si-JSON-invalide côté `recognitionController`, `go test -race` propre. Ce que ces tests ne couvrent pas : le vrai `RecognitionUseCase` (webcam/ONNX), déjà validé une fois manuellement (ci-dessus), pas par du code versionné.
- [x] **Deux bugs de shutdown réels trouvés et corrigés le 2026-08-11**, en testant le filtre hybride (item § A) en webcam réelle — détail complet `docs/adr/clip-backend.md` § 15. (1) **SIGSEGV confirmé** : le handler de shutdown gracieux détruisait `objectDetector`/`semanticEncoder` sans attendre la fin d'une session `RecognitionUseCase` en cours — crash reproduit dans `EncodeImage`/CGo pendant qu'une session tournait encore. Fix : `uc.UseCase` a un `sync.WaitGroup` interne, nouvelle méthode `UseCases.Wait()`, le handler de `main.go` appelle `Stop()` puis `Wait()` avant les `Cleanup()`. (2) **Process qui ne quittait jamais** après un shutdown "réussi" (logué comme terminé, mais port toujours ouvert, seul `SIGKILL` en venait à bout) — `gin.Engine.Run()` bloque indéfiniment, rien ne forçait la sortie. Fix : `startWebServer` attend sur `engine.Gracefull().Done()` et appelle `os.Exit(0)` explicitement. **Les deux corrigés et re-vérifiés avec le repro exact** (`stop` → `sleep 1` → `pkill`, avant/après). Probablement présents depuis le câblage initial de § H1 — pas détectés avant faute d'avoir vérifié `ps`/`lsof` après un kill, et la fenêtre de course du (1) ne s'ouvrait pas assez avec des cycles reanchor rapides (sans CLIP).
- [x] **Migration `DynamicAdvancedSession` — faite le 2026-08-11.** `yolo11s.Detector` (1 session) et `clip.Encoder` (2 sessions, vision + texte) : `*ort.AdvancedSession` → `*ort.DynamicAdvancedSession`, tenseurs input/output qui étaient des champs de struct (alloués une fois à `New()`, réutilisés par tous les appels) remplacés par des tenseurs alloués **par appel** dans `AnalyzeFrame`/`EncodeImage`/`EncodeText` (`ort.NewEmptyTensor` + `defer .Destroy()` locaux), passés à `session.Run(inputs, outputs []ort.Value)` au lieu de `session.Run()` sans argument. `writeInput`/`writeImage` désormais des fonctions libres prenant `[]float32` en paramètre plutôt que des méthodes lisant un champ `*Detector`/`*Encoder` — elles ne touchent plus d'état partagé. Poids du modèle toujours chargés une seule fois (la session elle-même), seuls les petits tenseurs par appel sont désormais éphémères — conforme à la décision § 1.2 de `docs/gui/spec.md` (thread-safety validée par le test `-race` qui a tranché cette architecture, pas re-testé ici en concurrence réelle puisque rien n'appelle encore `AnalyzeFrame`/`EncodeImage` concurremment avant le multi-flux). **Testé en conditions réelles** (webcam, `--web`, ~380 cycles reanchor sur 8s) : zéro erreur, timings inchangés (~230-320ms/cycle, cohérent avec la baseline pré-migration), arrêt propre sans SIGABRT — pas juste compilé. Coût de l'allocation par appel toujours pas benchmarké isolément (pas de régression de timing observée, mais pas mesuré précisément non plus) — reste un point à surveiller si le multi-flux charge davantage.
- [x] **Nouveaux adaptateurs `streamer.InputStream` — faits le 2026-08-11.** `CameraInput` : device désormais paramétrable (`NewCameraInput(device int)`, plus de `0` en dur) — pas encore exposé via CLI/REST (source selection = GUI "Ajouter une source", § H1 Multi-flux/H2), juste rendu possible côté adaptateur comme demandé. Nouveau `FileInput` (`internal/implementation/streamer/input/file.go`) : **un seul type pour "fichier vidéo local" ET "RTSP/caméra IP"**, pas deux — `gocv.VideoCaptureFile(uri)` résout les deux la même façon depuis le schéma de l'URI (chemin local, `rtsp://`, `http://`), même boucle de lecture, aucune raison de dupliquer. Boucle de lecture commune (`captureLoop`, `capture_loop.go`) extraite pour `CameraInput`/`FileInput` — seules différences réelles entre les deux : ouverture (device vs URI) et mirroring (webcam seulement, jamais un fichier/flux IP). **Testé réellement** : `FileInput` contre `assets/videos/car.mp4` (238 frames lues, 1280×720, cleanup propre) — 8 tests table-driven versionnés (`file_test.go`, dont un contre le vrai fichier, pas mocké) + 3 pour `CameraInput` (`camera_test.go`, ce qui est testable sans matériel réel : stockage du device, `Cleanup`/`Start` sans `Initialize`). **RTSP testé bout-en-bout le 2026-08-11**, via un nouvel outil dev **conservé dans le repo** (`cmd/rtsp-smoke-test`, pas supprimé après usage contrairement à la plupart des outils jetables du projet — pas de test automatisé possible pour RTSP, cf. ci-dessous, donc le relancer est le moyen de détecter une régression future) — pas de flux public fiable pour ça (ils tournent/disparaissent), donc auto-hébergé en local : `mediamtx` (`brew install mediamtx`) + `ffmpeg` qui boucle `assets/videos/car.mp4` dessus, `FileInput` pointé sur `rtsp://127.0.0.1:8554/car`. **Résultat sur plusieurs runs** : dimensions correctes (1280×720), cleanup propre, zéro erreur — mais **débit variable d'un run à l'autre** contre un publisher `-re` (rythme temps réel) fraîchement (re)démarré : de quelques frames sur les 5 premières secondes juste après un (re)démarrage à ~18-32 fps (proche du ~30fps source) une fois la session RTSP stabilisée quelques secondes. Warnings ffmpeg `Missing reference picture`/`mmco: unref short failure`/`co located POCs unavailable` au connect (attendu, raccord en plein milieu d'un GOP H.264 sur un flux en boucle, rien de spécifique à cet adaptateur). Pas de test automatisé versionné (dépendrait d'un serveur RTSP vendorisé ou d'un réseau live, aucun des deux ne colle aux conventions de test du projet) — `cmd/rtsp-smoke-test` en tient lieu. La reconnexion automatique sur coupure réseau (§ 1.3) reste non implémentée — documentée comme lacune connue, pas un oubli silencieux.
- [ ] Multi-flux : plusieurs `RecognitionUseCase` concurrents adressables par ID de session (un par onglet/tuile) — s'appuie sur l'item `DynamicAdvancedSession` ci-dessus. Mesurer la charge CPU réelle avant d'annoncer un nombre de flux simultanés supporté (aucun EP GPU câblé à ce jour, tout CPU, § 1.6).
- [ ] Ingestion WebRTC navigateur (webcam du navigateur → backend) — effort L, nouvelle dépendance `pion/webrtc`, prioritaire sur le fallback selon la décision actée. STUN/TURN à prévoir pour des sources non locales (§ 4 risques).
- [ ] Fallback JPEG-over-WS (effort S, peut être fait en parallèle de WebRTC) + flux YouTube/live via `yt-dlp` en shell-out (effort S, dépendance externe non-Go, non testée).
- [ ] Galerie de références + sélection runtime côté `UseCase` — **même chantier que § D "reconnaissance par référence image"** (décidé le 2026-08-10) : store `{label, embedding}` en mémoire alimenté par `EncodeImage` sur un crop sélectionné en direct, comparé par similarité cosinus à chaque détection. Ce qui manquait pour le démarrer était l'UI d'interaction en direct (rien de prévu dans le CLI) — H2 la fournit. Effort M, § 1.4/1.5 de `docs/gui/spec.md`. Nouveau cas d'usage ou extension de `RecognitionUseCase`, pas bricolé dans le transport.
- [ ] Pause/reprise + buffer de rewind par flux — **même chantier que "V2 ring buffer" déjà noté en § C** (pensé à l'origine pour le tracker, pas pour l'utilisateur) — à concevoir une seule fois pour les deux usages, pas deux implémentations séparées. Effort M, § 1.5bis de `docs/gui/spec.md`. Coût mémoire non chiffré, à valider avant de fixer une durée par défaut dans l'UI.
- [ ] Wrapper desktop Wails (nouvelle dépendance, jamais utilisée dans ce projet) — effort S, mais seulement une fois H2 fonctionnel (embarque la UI web telle quelle, pas de travail UI dupliqué).

### H2 — Frontend web (React + Vite + TypeScript, dossier `web/` — dépend intégralement de H1)

**Stack confirmé le 2026-08-12 : React + Vite + TypeScript** (`docs/gui/spec.md` § 0 le laissait ouvert). Découpage ci-dessous basé sur les maquettes reçues (`docs/gui/mockups/`, écrans 1a-1h) et `docs/gui/spec.md` § 3.

**Décision de séquencement 2026-08-12 : tranche verticale d'abord, pas 100% de H1 avant de commencer.** Aucun des items H1 restants (multi-flux, WebRTC, rewind, galerie) n'est un vrai bloquant pour un premier écran réel — le REST (start/stop/status) + WS (vidéo annotée) existants suffisent pour une source mono-caméra. Choix : scaffolder H2 + un premier écran contre l'existant, valider en conditions réelles, puis rattraper le reste de H1 au fur et à mesure plutôt que de tout bloquer derrière H1.

- [x] **Scaffolding projet (`web/`) — fait le 2026-08-12.** Vite + React + TypeScript (`npm create vite@latest web -- --template react-ts`), proxy dev `/api`+`/ws` → `localhost:8080` (`vite.config.ts`, `ws: true` requis pour le proxy WebSocket). **Premier écran fonctionnel** (pas le design system des maquettes — délibérément simplifié pour valider le transport d'abord, tokens Archivo/Space Mono/thème clair-sombre restent à faire, item suivant) : `App.tsx` (filtre texte + start/stop + badge de statut, poll `GET /recognition/status` toutes les 2s), `VideoStream.tsx` (consomme `/ws`, un message binaire JPEG par frame → `URL.createObjectURL`, révoqué à chaque nouvelle frame pour ne pas fuir), `api.ts` (wrapper REST typé). Build (`tsc -b && vite build`) et lint (`oxlint`) propres, dev server démarre et sert `200` (vérifié via `curl`). **Pas testé avec une vraie session de reconnaissance ni visuellement** (pas de webcam/affichage dans cet environnement) — à faire par l'utilisateur : `go run ./cmd/livesemantic --web` + `cd web && npm run dev`.
  - **Source vidéo actuelle = caméra locale du backend, pas caméra navigateur — distinction clarifiée avec l'utilisateur.** Caméra locale = le process Go ouvre directement la caméra de la machine où *lui* tourne (`CameraInput`/gocv) ; caméra navigateur = le navigateur capture via `getUserMedia` et **envoie** le flux au backend par le réseau. Les deux semblent identiques en test local (même Mac pour le backend et le navigateur) mais sont architecturalement différentes — seule la caméra navigateur permet à une vraie app web d'être utilisée depuis n'importe quel appareil (téléphone, autre laptop), pas seulement la machine qui héberge le backend. **L'utilisateur confirme vouloir la caméra navigateur** ("si c'est une app web, adaptable sur tout device") — prochaine étape après ce scaffolding, priorité sur le reste de H2. Piste retenue : JPEG-over-WS d'abord (capture `getUserMedia`+`canvas`, POST/WS d'une frame JPEG, effort S, zéro nouvelle dépendance Go) avant le vrai WebRTC (`pion/webrtc`, effort L, meilleure latence/qualité) — cohérent avec la priorité déjà actée dans `docs/gui/spec.md` § 2.
- [x] **Capture caméra navigateur (JPEG-over-WS) — implémentée et testée en conditions réelles le 2026-08-12.** Backend : nouvel adapter `implementation/streamer/input.BrowserInput` (`streamer.InputStream` alimenté de l'extérieur via `PushFrame`, pas d'ouverture directe d'un device — channel buffer-1 overwrite-on-full, même pattern que le hand-off frame de `uc_recognition.go`) ; nouvel endpoint `GET /ws/ingest` (`transport/adapters/api`, décode le JPEG binaire reçu, appelle `PushFrame`) ; `dto.RecognitionRequest.Source` (`"local"`/`"browser"`, défaut `"local"` — chaque appelant existant continue de marcher sans changement) ; `uc.UseCase` n'a plus un seul `streamingInput` mais `localInput`/`browserInput` nommés + `activeInput` (mutex-gardé) pour que `Stop()` arrête le bon input, pas toujours le local. **Race détectée par `go test -race`** sur `BrowserInput.running`/`.stop` (accès concurrent Start/Stop) — corrigée avec un mutex dédié, capture d'une référence locale stable du channel stop au début de `Start()`. Frontend : `BrowserCamera.ts` (`getUserMedia` → `<video>` caché → `<canvas>` → JPEG toutes les 150ms → push sur `/ws/ingest`), toggle caméra serveur/navigateur dans `App.tsx`. **Testé de bout en bout en conditions réelles** (pas juste compilé) : client WS Python maison poussant une vraie frame JPEG (extraite de `assets/videos/person.mp4` via ffmpeg) sur `/ws/ingest` avec une session `source: browser` active — logs confirmant connexion, décodage, boucle vidéo ET boucle de détection (`reanchor`, ~180-196ms, cohérent avec un vrai passage YOLO) tournant sur les frames poussées ; arrêt propre sans SIGABRT/SIGSEGV. Chemin caméra locale (`source` omis) re-testé en parallèle pour non-régression — `active_tracks: 1` confirmé (vraie détection "person"), arrêt propre aussi. `go vet`/`gofmt`/`go test -race` propres sur tout le repo. **Pas testé avec un vrai navigateur/webcam** (aucun navigateur interactif dans cet environnement) — `BrowserCamera.ts` (`getUserMedia`) reste à valider visuellement par l'utilisateur.
- [ ] Système de design de base (tokens couleur/typo Archivo+Space Mono, composants réutilisables — cf. écran 1h : boutons, badges de statut, box vidéo overlay, slider+preview, carte source) — pas fait, l'écran actuel est un CSS minimal fonctionnel, pas le design des maquettes.
- [ ] Thème clair/sombre, toggle accessible (pas enfoui) — les deux variantes complètes dès le départ, pas un sombre par défaut avec un clair ajouté après coup.
- [ ] Accueil "Sources" — vue liste (1b) : une ligne par source, statut, filtres actifs, dernier événement, actions (ouvrir/éditer/supprimer).
- [ ] Accueil "Sources" — vue mosaïque (1a/1e) + volet réglages repliable (boxes sur tuiles on/off, FPS des tuiles, indicateur de charge d'affichage) — **dépend du choix mosaïque câblé côté WS** (abonnement par flux avec fps/boxes ajustables, pas deux tiers figés preview/complet, § 2 revu 2026-08-10).
- [ ] Ajout/édition/suppression de source (formulaire, esquissé en flou dans les maquettes — pas demandé au design, à concevoir).
- [ ] Onglet Vue live (1c/1f) : rendu vidéo + boxes + scores en direct (WS), barre de transport (pause/reprise/±10s/retour au direct/tampon configurable) — dépend du buffer de rewind H1.
- [ ] Sélection runtime + labellisation (1d) : clic sur une box existante ou dessin à main levée → mini-formulaire label/couleur → ajout à la galerie — dépend de la galerie de références + sélection runtime H1.
- [ ] Volet droit — filtres sémantiques : champ texte libre multi-filtres, toggle par filtre, slider de seuil **avec retour de score en direct** (sparkline temps réel pendant le déplacement du curseur, avant validation — interaction la plus différenciante du produit, à soigner en particulier), raccourci "appliquer à toutes les sources actives".
- [ ] Volet droit — galerie de références : liste des entrées (renommer/activer-désactiver/supprimer), couleur de box configurable par entrée (pas par catégorie COCO).
- [ ] Volet droit — section "avancés" repliée par défaut : algorithme de tracking (KCF/CSRT), IoU d'association, frames avant perte, matériel d'inférence (EP), variante de modèle — tous existent déjà en dur côté backend (tableau `docs/gui/spec.md` § 3.4), juste à exposer une fois les endpoints REST prêts.
- [ ] Journal des événements (tiroir accessible depuis la barre du bas) : liste agrégée multi-flux, filtrable par source — emplacement (accueil vs par onglet) encore un point ouvert côté design.
- [ ] États dégradés : "aucun flux configuré", flux en erreur/reconnexion — doivent rester lisibles, pas juste des cas heureux.
- [ ] Overlay de performance optionnel et discret sur la Vue live (FPS, latence, nb d'objets suivis) — masquable, ne doit jamais gêner la lecture de l'image.

### H3 — Desktop (dépend de H2 fonctionnel)

- [ ] Intégration Wails autour de la UI web H2 — lancement du backend en local au démarrage, packaging par OS cible (non précisé). Pas de travail UI dupliqué par construction (même code que H2).

---

## Hors-scope de ce backlog

- **Branches à trancher** (`realtime-uc`, fast-forward `main`) : décisions utilisateur, voir `AUDIT.md` § Questions. `feat/ochestrator` est tranchée : non récupérée (le commit dangling `4c482dc` a été supprimé volontairement, pas de branche de secours).
